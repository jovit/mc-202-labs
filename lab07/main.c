#include <stdio.h>

int main(void) {
    int cache_size, number_of_elements, number_of_access;

    scanf("%d %d %d", &cache_size, &number_of_elements, &number_of_access);

    // read all access and store on queue
    // cache represented by a priority queue

    return 0;
}